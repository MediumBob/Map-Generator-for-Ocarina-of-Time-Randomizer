/**
 * Builds up our graph object
 * @param {object} graphDetails - dictionary storing details for each node in our graph
 * @returns our graph object
 */
function Mapper(graphDetails){
    let graph = "!"

    return graph
}

export default Mapper